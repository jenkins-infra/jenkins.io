---
layout: advisory
title: Jenkins Security Advisory 2026-06-24
kind: plugins
issues:
- id: SECURITY-3792
  title: Sandbox bypass vulnerability in PLUGIN_NAME
  cve: CVE-2026-57280
  cvss:
    severity: High
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:H
  description: |-
    PLUGIN_NAME provides a sandbox feature that allows running user-provided scripts safely by intercepting and checking potentially unsafe operations.

    PLUGIN_NAME 1402.v94c9ce464861 and earlier does not intercept the implicit type cast applied to each element of the iterated collection in a typed `for` loop (e.g. `for (Type t in collection)`), as this cast is performed during bytecode generation rather than in the transformed script AST.

    This allows attackers able to provide sandboxed scripts to invoke constructors of arbitrary types without those invocations being checked by the sandbox, bypassing the sandbox protection. This can be used to execute arbitrary code on the Jenkins controller.

    PLUGIN_NAME 1402.1405.vc96e74964250 updates the bundled `groovy-sandbox` library to a version that intercepts the implicit type cast applied to typed `for` loop elements, so those casts are checked by the sandbox.
  plugins:
  - name: script-security
    previous: 1402.v94c9ce464861
    fixed: 1402.1405.vc96e74964250
- id: SECURITY-3793
  title: Script security bypass vulnerability in PLUGIN_NAME
  cve: CVE-2026-57281
  cvss:
    severity: High
    vector: CVSS:3.1/AV:N/AC:H/PR:L/UI:N/S:U/C:H/I:H/A:H
  description: |-
    PLUGIN_NAME 1402.v94c9ce464861 and earlier does not reject Groovy AST transformation annotations such as `@CompileStatic` and `@TypeChecked` that carry an `extensions` member, which causes Groovy to load and execute a script from the classpath at compile time, before the sandbox is applied.

    This may allow attackers able to define and run sandboxed scripts to execute code outside the sandbox, in the rare case that a suitable Groovy script is present on the classpath of the component that evaluates the script.

    NOTE: The Jenkins security team has been unable to identify any Groovy source files in Jenkins core or plugins that would allow attackers to execute dangerous code. While the severity of this issue is declared as High due to the potential impact, successful exploitation is considered very unlikely.

    PLUGIN_NAME 1402.1405.vc96e74964250 rejects any annotation carrying an `extensions` member during sandbox compilation, before Groovy can resolve or execute the referenced script.
  plugins:
  - name: script-security
    previous: 1402.v94c9ce464861
    fixed: 1402.1405.vc96e74964250
- id: SECURITY-3723
  reporter: Ravindu Wickramasinghe
  title: OS command injection vulnerability on agents in PLUGIN_NAME
  cve: CVE-2026-57282
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:H/PR:L/UI:N/S:U/C:L/I:L/A:L
  description: |-
    PLUGIN_NAME 6.6.0 and earlier does not correctly escape the workspace directory name when it is embedded into the SSH wrapper script generated by the "Manually provided keys" Git Host Key Verification strategy on Unix agents.

    This allows attackers able to control the name of a build's working directory (e.g. through a build parameter that determines the workspace directory) to inject shell command substitution and execute arbitrary commands on the agent.

    NOTE: This vulnerability only has an impact when attackers can control working directories (e.g., the argument to the `dir(…)` Pipeline step) while not being able to control the Pipeline itself or the programs or build scripts it executes.

    NOTE: This vulnerability has been reported through the link:/blog/2025/12/10/jenkins-bug-bounty-program-yeswehack-european-commission/[Jenkins Bug Bounty Program sponsored by the European Commission].

    PLUGIN_NAME 6.6.1 stores the known_hosts file used by the "Manually provided keys" Git Host Key Verification strategy in the system temporary directory, so the workspace directory name is no longer embedded in the path passed to the generated SSH wrapper script.
  plugins:
  - name: git-client
    previous: 6.6.0
    fixed: 6.6.1
- id: SECURITY-3677
  title: CSRF vulnerability and unrestricted instantiation of types in PLUGIN_NAME
  cve: CVE-2026-57283 (CSRF), CVE-2026-57284 (unrestricted instantiation of types)
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:N/I:L/A:N
  description: |-
    PLUGIN_NAME 4331.v9d06ed4658ff and earlier does not restrict the types that can be instantiated through the Pipeline Snippet Generator, instantiating any type with a constructor annotated with `@DataBoundConstructor` in response to a request.

    This allows attackers to have PLUGIN_NAME instantiate types related to job or system configuration other than Pipeline steps.

    Additionally, this HTTP endpoint can be accessed using the GET method and does not require POST requests, resulting in a cross-site request forgery (CSRF) vulnerability.
    This allows attackers to create a script approval request attributed to another user, impersonating a trusted user when social engineering an administrator into approving a malicious script.

    NOTE: This vulnerability has been reported through the link:/blog/2025/12/10/jenkins-bug-bounty-program-yeswehack-european-commission/[Jenkins Bug Bounty Program sponsored by the European Commission].

    PLUGIN_NAME 4331.4333.v50a_b_076c5199 only instantiates Pipeline steps and metastep delegates through the Snippet Generator, and requires POST requests for the affected HTTP endpoint.
  plugins:
  - name: workflow-cps
    previous: 4331.v9d06ed4658ff
    fixed: 4331.4333.v50a_b_076c5199
- id: SECURITY-3808
  reporter: Suman Roy (https://linkedin.com/in/sumanrox)
  title: Missing permission check allows enumerating GitHub Enterprise server URLs
    in PLUGIN_NAME
  cve: CVE-2026-57285
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:L/I:N/A:N
  description: |-
    PLUGIN_NAME 1967.1969.v205fd594c821 and earlier does not perform a permission check in an HTTP endpoint that lists the GitHub API endpoints configured in the global plugin configuration.

    This allows attackers with Overall/Read permission to obtain the URLs of GitHub Enterprise servers configured by administrators.

    NOTE: This vulnerability has been reported through the link:/blog/2025/12/10/jenkins-bug-bounty-program-yeswehack-european-commission/[Jenkins Bug Bounty Program sponsored by the European Commission].

    PLUGIN_NAME 1967.1970.vd86979736546 requires Overall/Manage permission or Item/Extended Read permission on an item to list the configured GitHub API endpoints.
  plugins:
  - name: github-branch-source
    previous: 1967.1969.v205fd594c821
    fixed: 1967.1970.vd86979736546
- id: SECURITY-3745
  reporter: SungpilHan (@EQSTLab)
  title: Missing permission check in PLUGIN_NAME allows listing SCM branch and tag
    names
  cve: CVE-2026-57286
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:L/I:N/A:N
  description: |-
    PLUGIN_NAME 462.vdcf3df2ed2ca_ and earlier does not perform a permission check in an HTTP endpoint that populates the list of values for Git parameters by querying the SCM configured on a job, using the SCM credentials configured in Jenkins.

    This allows attackers with Item/Read permission to obtain information about the SCM repository used by a job they would otherwise be unable to access, such as branch names, tag names, and revision metadata.

    PLUGIN_NAME 462.463.v496a_59f698e5 requires Item/Build permission to populate the list of values for Git parameters.
  plugins:
  - name: git-parameter
    previous: 462.vdcf3df2ed2ca_
    fixed: 462.463.v496a_59f698e5
- id: SECURITY-3742
  reporter: Ophion Security in collaboration with Claude and Anthropic Research
  title: Encrypted values of secrets in job and agent configurations not redacted
    by PLUGIN_NAME
  cve: CVE-2026-57287
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:L/I:N/A:N
  description: |-
    PLUGIN_NAME 1356.ve360da_6c523a_ and earlier does not redact the encrypted values of secrets when displaying historical job and agent configurations through its "View as XML" / "(RAW)" feature and its configuration diff views.

    This allows attackers with Item/Extended Read permission (but not Item/Configure permission) to view the encrypted values of secrets, such as build trigger tokens, that Jenkins would otherwise redact from the configuration shown to them.

    PLUGIN_NAME 1367.vc8fa_b_15101dc redacts the encrypted values of secrets when displaying historical job and agent configurations through its "View as XML" / "(RAW)" feature and its configuration diff views to users lacking Item/Configure permission.
  plugins:
  - name: jobConfigHistory
    previous: 1356.ve360da_6c523a_
    fixed: 1367.vc8fa_b_15101dc
- id: SECURITY-3651
  reporter: Arad Inbar, Ben Grinberg, Nir Somech from DREAM, and, independently, Nahit
    Sogutlu (http://github.com/Dogru-Isim)
  title: LDAP injection vulnerability in PLUGIN_NAME
  cve: CVE-2026-57288
  cvss:
    severity: Low
    vector: CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:L/I:N/A:N
  description: |-
    In PLUGIN_NAME 2.41.1 and earlier, the Windows native (ADSI) authentication path does not escape the user name before building the LDAP search filter.

    This allows unauthenticated attackers to inject LDAP wildcard characters into the user name, enabling them to enumerate directory user and group names, and to authenticate as a matching user when they know that user's password but not their exact user name.

    PLUGIN_NAME 2.41.2 escapes the user name in the Windows native (ADSI) authentication path before building the LDAP search filter.
  plugins:
  - name: active-directory
    previous: 2.41.1
    fixed: 2.41.2
- id: SECURITY-3759
  reporter: Heechan, and, independently, YeJun Won
  title: Missing permission check in PLUGIN_NAME allows reading Pipeline replay scripts
  cve: CVE-2026-57300
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:L/I:N/A:N
  description: |-
    PLUGIN_NAME 0.177.v629fdb_2557fe and earlier does not perform a permission check in the `getReplayScripts` MCP tool that returns the replay script of a Pipeline build.

    This allows attackers with Item/Read permission to obtain the Pipeline script of jobs.

    PLUGIN_NAME 0.178.vffe5a_e770f3b_ requires Item/Extended Read permission to return the replay script of a Pipeline build through the `getReplayScripts` MCP tool.
  plugins:
  - name: mcp-server
    previous: 0.177.v629fdb_2557fe
    fixed: 0.178.vffe5a_e770f3b_
- id: SECURITY-3856
  reporter: dyingman1 (https://github.com/dyingman1, redpoc Offensive Security Team)
  title: SSL/TLS certificate validation unconditionally disabled by PLUGIN_NAME
  cve: CVE-2026-57289
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:L/I:L/A:N
  description: |-
    PLUGIN_NAME 3.3.8 and earlier unconditionally disables SSL/TLS certificate and hostname validation for the connections it makes to Bitbucket Server using Bearer token authentication.

    Because the Bearer token is transmitted in these requests, this allows attackers able to intercept network traffic to capture the token and impersonate the Jenkins controller to Bitbucket Server.

    PLUGIN_NAME 3.3.9 validates SSL/TLS certificates and hostnames for the connections it makes to Bitbucket Server using Bearer token authentication, using the trust store configured for the Jenkins controller JVM.
  plugins:
  - name: bitbucket-push-and-pull-request
    previous: 3.3.8
    fixed: 3.3.9
- id: SECURITY-3769
  reporter: dyingman1 (https://github.com/dyingman1, redpoc Offensive Security Team)
  title: CSRF vulnerability in PLUGIN_NAME
  cve: CVE-2026-57290
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:N/I:L/A:N
  description: |-
    PLUGIN_NAME 936.v2c01c6b_84449 and earlier does not require POST requests in an HTTP endpoint that saves the global job priority configuration.

    This allows attackers to overwrite the global job priority configuration.

    PLUGIN_NAME 936.937.v5581d0b_2ccb_a_ requires POST requests for the affected HTTP endpoint.
  plugins:
  - name: PrioritySorter
    previous: 936.v2c01c6b_84449
    fixed: 936.937.v5581d0b_2ccb_a_
- id: SECURITY-3762 (1)
  reporter: dyingman1 (https://github.com/dyingman1, redpoc Offensive Security Team)
  title: Missing permission checks and CSRF vulnerability in PLUGIN_NAME
  cve: CVE-2026-57291 (missing permission check), CVE-2026-57292 (CSRF)
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:L/I:L/A:N
  description: |-
    PLUGIN_NAME 1288.v18b_deb_c9069b_ and earlier does not perform permission checks in several HTTP endpoints implementing form validation for its global configuration.

    This allows attackers with Overall/Read permission to connect to an attacker-specified URL using attacker-specified credentials IDs obtained through another method, capturing credentials stored in Jenkins.

    Additionally, these HTTP endpoints do not require POST requests, resulting in a cross-site request forgery (CSRF) vulnerability.

    PLUGIN_NAME 1292.v2559f2f3f2c0 requires the appropriate permissions in the affected HTTP endpoints, and requires POST requests.
  plugins:
  - name: gitee
    previous: 1288.v18b_deb_c9069b_
    fixed: 1292.v2559f2f3f2c0
- id: SECURITY-3762 (2)
  reporter: dyingman1 (https://github.com/dyingman1, redpoc Offensive Security Team)
  title: Incorrect permission check in PLUGIN_NAME allows enumerating credentials
    IDs
  cve: CVE-2026-57293
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:L/I:N/A:N
  description: |-
    PLUGIN_NAME 1288.v18b_deb_c9069b_ and earlier does not correctly perform a permission check in an HTTP endpoint.

    This allows attackers with global Item/Configure permission (while lacking Item/Configure permission on any particular job) to enumerate credentials IDs of credentials stored in Jenkins.
    Those can be used as part of an attack to capture the credentials using another vulnerability.

    An enumeration of credentials IDs in PLUGIN_NAME 1292.v2559f2f3f2c0 requires Overall/Administer permission.
  plugins:
  - name: gitee
    previous: 1288.v18b_deb_c9069b_
    fixed: 1292.v2559f2f3f2c0
- id: SECURITY-3774
  reporter: dyingman1 (https://github.com/dyingman1, redpoc Offensive Security Team)
  title: CSRF vulnerability and missing permission checks in PLUGIN_NAME
  cve: CVE-2026-57294 (missing permission check), CVE-2026-57295 (CSRF)
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:L/I:L/A:N
  description: |-
    PLUGIN_NAME 4.2.3.539.v8fedff2a_81c3 and earlier does not perform permission checks in several HTTP endpoints used to validate cloud configurations.

    This allows attackers with Overall/Read permission to connect to an attacker-specified URL using attacker-specified credentials IDs obtained through another method, capturing AWS credentials stored in Jenkins.

    Additionally, these HTTP endpoints do not require POST requests, resulting in a cross-site request forgery (CSRF) vulnerability.

    PLUGIN_NAME 4.2.3.540.va_6eedb_7b_c112 requires Overall/Administer permission and POST requests to perform these form validation actions.
  plugins:
  - name: ec2-fleet
    previous: 4.2.3.539.v8fedff2a_81c3
    fixed: 4.2.3.540.va_6eedb_7b_c112
- id: SECURITY-3777
  reporter: dyingman1 (https://github.com/dyingman1, redpoc Offensive Security Team)
  title: Path traversal vulnerability in PLUGIN_NAME
  cve: CVE-2026-57296
  cvss:
    severity: High
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:H
  description: |-
    PLUGIN_NAME 1.3.2 and earlier does not reject `..` path segments when validating the custom workspace path provided to the `exwsAllocate` Pipeline step, allowing the resulting workspace path to escape the configured disk mount point.

    This allows attackers with Item/Configure permission to read arbitrary files on the Jenkins controller file system, which can lead to remote code execution (see link:/security/vulnerabilities/#reading[Reading Files]).

    PLUGIN_NAME 1.4.0 rejects `..` path segments when validating the custom workspace path, and additionally verifies that the requested path is contained within the configured disk mount point before serving it through the external workspace browse functionality.
  plugins:
  - name: external-workspace-manager
    previous: 1.3.2
    fixed: 1.4.0
- id: SECURITY-3697 (1)
  reporter: Kai Aizen (SnailSploit)
  title: CSRF vulnerability and missing permission check in PLUGIN_NAME
  cve: CVE-2026-57297 (missing permission check), CVE-2026-57298 (CSRF)
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:L/I:L/A:N
  description: |-
    PLUGIN_NAME 3.11 and earlier does not perform a permission check in an HTTP endpoint that tests the connection to a Contrast TeamServer.

    This allows attackers with Overall/Read permission to connect to an attacker-specified URL using an attacker-specified username, API key, and service key.

    Additionally, this HTTP endpoint does not require POST requests, resulting in a cross-site request forgery (CSRF) vulnerability.

    PLUGIN_NAME 3.12 requires Overall/Administer permission and POST requests to test the connection to a Contrast TeamServer.
  plugins:
  - name: contrast-continuous-application-security
    previous: '3.11'
    fixed: '3.12'
- id: SECURITY-3697 (2)
  reporter: Kai Aizen (SnailSploit)
  title: Missing permission checks in PLUGIN_NAME allow enumerating Contrast metadata
  cve: CVE-2026-57299
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:L/I:N/A:N
  description: |-
    PLUGIN_NAME 3.11 and earlier does not perform permission checks in several HTTP endpoints that fill list box options with the names of the configured Contrast metadata.

    This allows attackers with Overall/Read permission to enumerate the names of configured Contrast metadata.

    PLUGIN_NAME 3.12 requires the appropriate permission to enumerate the configured Contrast metadata.
  plugins:
  - name: contrast-continuous-application-security
    previous: '3.11'
    fixed: '3.12'
- id: SECURITY-3649
  reporter: Pablo Picurelli Ortiz (superpegaso2703) of Universidad Rey Juan Carlos
  title: Builds executed on the Jenkins controller by PLUGIN_NAME can lead to RCE
  cve: CVE-2026-57301
  cvss:
    severity: High
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:H
  description: |-
    PLUGIN_NAME 1.0.7 and earlier does not support distributed builds, causing the file operations and build process of its "Automatically build ZAP" feature to be performed on the Jenkins controller rather than on the agent the build is assigned to.

    This allows attackers with Item/Configure permission to configure the feature to build an attacker-controlled project, executing arbitrary code on the Jenkins controller and bypassing any restriction confining the build to a specific agent.

    As of publication of this advisory, there is no fix.
    link:/security/plugins/#unresolved[Learn why we announce this.]
  plugins:
  - name: zapper
    previous: 1.0.7
- id: SECURITY-3555
  reporter: Romuald Moisan, Aix Marseille University
  title: Passwords stored in plain text by PLUGIN_NAME
  cve: CVE-2026-57302
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:L/I:N/A:N
  description: |-
    PLUGIN_NAME 1.36 and earlier stores passwords unencrypted in job `config.xml` files on the Jenkins controller as part of its configuration.

    These passwords can be viewed by users with Item/Extended Read permission or access to the Jenkins controller file system.

    As of publication of this advisory, there is no fix.
    link:/security/plugins/#unresolved[Learn why we announce this.]
  plugins:
  - name: fitnesse
    previous: '1.36'
- id: SECURITY-3692 (1)
  reporter: Kai Aizen (SnailSploit)
  title: XXE vulnerability in PLUGIN_NAME
  cve: CVE-2026-57303
  cvss:
    severity: High
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:L/A:N
  description: |-
    PLUGIN_NAME 1.4 and earlier does not configure its XML parser to prevent XML external entity (XXE) attacks when parsing responses from the configured Assembla server.

    This allows attackers able to control the responses of the configured Assembla server to extract secrets from the Jenkins controller or perform server-side request forgery.

    As of publication of this advisory, there is no fix.
    link:/security/plugins/#unresolved[Learn why we announce this.]
  plugins:
  - name: assembla
    previous: '1.4'
- id: SECURITY-3692 (2)
  reporter: Kai Aizen (SnailSploit)
  title: CSRF vulnerability and missing permission check in PLUGIN_NAME
  cve: CVE-2026-57304 (missing permission check), CVE-2026-57305 (CSRF)
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:L/I:L/A:N
  description: |-
    PLUGIN_NAME 1.4 and earlier does not perform a permission check in an HTTP endpoint that tests the connection to an Assembla server.

    This allows attackers with Overall/Read permission to connect to an attacker-specified URL using an attacker-specified username and password.

    Additionally, this HTTP endpoint does not require POST requests, resulting in a cross-site request forgery (CSRF) vulnerability.

    NOTE: This does not allow exploiting the XML external entity (XXE) vulnerability described in the previous advisory entry.

    As of publication of this advisory, there is no fix.
    link:/security/plugins/#unresolved[Learn why we announce this.]
  plugins:
  - name: assembla
    previous: '1.4'
- id: SECURITY-3747
  reporter: dyingman1 (https://github.com/dyingman1, redpoc Offensive Security Team)
  title: CSRF vulnerability and missing permission check in PLUGIN_NAME
  cve: CVE-2026-57306 (CSRF), CVE-2026-57307 (missing permission check)
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:H/PR:L/UI:N/S:U/C:L/I:L/A:N
  description: |-
    PLUGIN_NAME 1.1.3.50.ve350c9b_450b_1 and earlier does not perform a permission check in an HTTP endpoint implementing a connection test.

    This allows attackers with Overall/Read permission to connect to an attacker-specified URL using attacker-specified credentials IDs obtained through another method, capturing credentials stored in Jenkins.

    Additionally, this HTTP endpoint does not require POST requests, resulting in a cross-site request forgery (CSRF) vulnerability.

    As of publication of this advisory, there is no fix.
    link:/security/plugins/#unresolved[Learn why we announce this.]
  plugins:
  - name: zdevops
    previous: 1.1.3.50.ve350c9b_450b_1
