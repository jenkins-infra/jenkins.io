---
layout: advisory
title: Jenkins Security Advisory 2026-09-16
kind: plugins
issues:
- id: SECURITY-3929
  reporter: Samy Medjahed (Ap4sh) & Eliott Laurie (Ethicxz)
  title: Sandbox bypass vulnerability in PLUGIN_NAME
  cve: CVE-2026-92122
  cvss:
    severity: High
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:H
  description: |-
    PLUGIN_NAME provides a sandbox feature that allows running user-provided scripts safely by intercepting and checking potentially unsafe operations.

    Groovy coerces a value to an interface by creating a proxy that forwards each interface method call to a method of the same name on that value.

    PLUGIN_NAME 1415.v9a_f9b_3a_c253d and earlier does not check the method that the proxy calls when the value inherits a method of the same name as an interface method.

    This allows attackers with permission to define and run sandboxed scripts, including Pipelines, to bypass the sandbox protection and execute arbitrary code in the context of the Jenkins controller JVM.

    PLUGIN_NAME 1422.v06869826dd9b_ rejects the coercion of a value to an interface when the value inherits a method of the same name as an interface method.

    NOTE: This vulnerability has been reported through the link:/blog/2025/12/10/jenkins-bug-bounty-program-yeswehack-european-commission/[Jenkins Bug Bounty Program sponsored by the European Commission].
  plugins:
  - name: script-security
    previous: 1415.v9a_f9b_3a_c253d
    fixed: 1422.v06869826dd9b_
- id: SECURITY-3931
  reporter: Samy Medjahed (Ap4sh) & Eliott Laurie (Ethicxz)
  title: Sandbox bypass vulnerability in PLUGIN_NAME
  cve: CVE-2026-92123
  cvss:
    severity: High
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:H
  description: |-
    PLUGIN_NAME provides a sandbox feature that allows running user-provided scripts safely by intercepting and checking potentially unsafe operations.

    PLUGIN_NAME 1415.v9a_f9b_3a_c253d and earlier does not intercept operations performed on a `null` receiver (method calls, property and attribute accesses, and array accesses).

    This allows attackers with permission to define and run sandboxed scripts, including Pipelines, to bypass the sandbox protection and execute arbitrary code in the context of the Jenkins controller JVM.

    PLUGIN_NAME 1422.v06869826dd9b_ intercepts operations performed on a `null` receiver.

    NOTE: This vulnerability has been reported through the link:/blog/2025/12/10/jenkins-bug-bounty-program-yeswehack-european-commission/[Jenkins Bug Bounty Program sponsored by the European Commission].
  plugins:
  - name: script-security
    previous: 1415.v9a_f9b_3a_c253d
    fixed: 1422.v06869826dd9b_
- id: SECURITY-3923
  reporter: Samy Medjahed (Ap4sh) & Eliott Laurie (Ethicxz)
  title: Sandbox bypass vulnerability in PLUGIN_NAME
  cve: CVE-2026-92124
  cvss:
    severity: High
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:H
  description: |-
    PLUGIN_NAME provides a sandbox feature that allows running user-provided scripts safely by intercepting and checking potentially unsafe operations.

    Groovy casts a collection to another type using the elements of that collection, either passing them to a constructor of that type or casting each of them.

    PLUGIN_NAME 1415.v9a_f9b_3a_c253d and earlier checks the operations Groovy will perform with the elements it reads from the collection, but performs the cast on the collection itself rather than on the elements it checked.

    This allows attackers with permission to define and run sandboxed scripts, including Pipelines, to have the cast performed on different elements by overriding the methods of a collection, bypassing the sandbox protection and executing arbitrary code in the context of the Jenkins controller JVM.

    PLUGIN_NAME 1422.v06869826dd9b_ performs the cast on the same elements it checked, rather than on the collection itself.

    NOTE: This vulnerability has been reported through the link:/blog/2025/12/10/jenkins-bug-bounty-program-yeswehack-european-commission/[Jenkins Bug Bounty Program sponsored by the European Commission].
  plugins:
  - name: script-security
    previous: 1415.v9a_f9b_3a_c253d
    fixed: 1422.v06869826dd9b_
- id: SECURITY-3925 (1)
  reporter: Samy Medjahed (Ap4sh) & Eliott Laurie (Ethicxz); and, independently, Charles
    Vosburgh
  title: Sandbox bypass vulnerability in PLUGIN_NAME
  cve: CVE-2026-92125
  cvss:
    severity: High
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:H
  description: |-
    PLUGIN_NAME provides a sandbox feature that allows running user-provided scripts safely by intercepting and checking potentially unsafe operations.

    PLUGIN_NAME 1415.v9a_f9b_3a_c253d and earlier does not reject the `@GroovyASTTransformationClass` annotation, which a script can use to link an annotation type it declares to an arbitrary AST transformation, causing Groovy to run that transformation at compile time, before the sandbox is applied.

    This allows attackers with permission to define and run sandboxed scripts, including Pipelines, to bypass the sandbox protection and execute arbitrary code in the context of the Jenkins controller JVM.

    PLUGIN_NAME 1422.v06869826dd9b_ rejects the `@GroovyASTTransformationClass` annotation during sandbox compilation, before Groovy can resolve or execute the referenced script.

    NOTE: This vulnerability has been reported through the link:/blog/2025/12/10/jenkins-bug-bounty-program-yeswehack-european-commission/[Jenkins Bug Bounty Program sponsored by the European Commission].
  plugins:
  - name: script-security
    previous: 1415.v9a_f9b_3a_c253d
    fixed: 1422.v06869826dd9b_
- id: SECURITY-3925 (2)
  reporter: Albert Puig Mercadé, CloudBees, Inc.
  title: Sandbox bypass vulnerability in PLUGIN_NAME
  cve: CVE-2026-92126
  cvss:
    severity: High
    vector: CVSS:3.1/AV:N/AC:H/PR:L/UI:N/S:U/C:H/I:H/A:H
  description: |-
    PLUGIN_NAME provides a sandbox feature that allows running user-provided scripts safely by intercepting and checking potentially unsafe operations.

    Groovy's `@Builder` annotation generates builder code at compile time using the strategy class named by its `builderStrategy` member, which can be one of the strategies provided by Groovy or a custom implementation.

    PLUGIN_NAME 1415.v9a_f9b_3a_c253d and earlier does not reject `@Builder` annotations whose `builderStrategy` member names an arbitrary class, which causes Groovy to instantiate that class at compile time, before the sandbox is applied.

    This may allow attackers with permission to define and run sandboxed scripts, including Pipelines, to execute code outside the sandbox, in the rare case that a suitable class is present on the classpath of the component that evaluates the script.

    PLUGIN_NAME 1422.v06869826dd9b_ rejects `@Builder` annotations whose `builderStrategy` member names a class other than the builder strategies provided by Groovy during sandbox compilation, before Groovy can instantiate it.

    NOTE: Sandboxed scripts can no longer refer to one of the builder strategies provided by Groovy through a star import, but can still refer to it by fully qualified name or through a single-class import.
  plugins:
  - name: script-security
    previous: 1415.v9a_f9b_3a_c253d
    fixed: 1422.v06869826dd9b_
- id: SECURITY-3897
  title: Classpath approval bypass vulnerability in PLUGIN_NAME
  cve: CVE-2026-92127
  cvss:
    severity: High
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:R/S:U/C:H/I:H/A:H
  description: |-
    PLUGIN_NAME allows Groovy scripts to load classpath entries from JAR files, which administrators must approve before any script is allowed to use them.

    PLUGIN_NAME 1415.v9a_f9b_3a_c253d and earlier automatically approves the classpath entries in an item configuration when a user with Overall/Administer permission copies the item, or updates that configuration through the REST API or CLI, allowing attackers able to define classpath entries to execute arbitrary code in the context of the Jenkins controller JVM.

    PLUGIN_NAME 1422.v06869826dd9b_ no longer automatically approves the classpath entries in an item configuration when a user with Overall/Administer permission copies the item or updates that configuration through the REST API or CLI.
  plugins:
  - name: script-security
    previous: 1415.v9a_f9b_3a_c253d
    fixed: 1422.v06869826dd9b_
- id: SECURITY-3932
  reporter: Samy Medjahed (Ap4sh) & Eliott Laurie (Ethicxz)
  title: TOCTOU race condition in PLUGIN_NAME
  cve: CVE-2026-92128
  cvss:
    severity: High
    vector: CVSS:3.1/AV:N/AC:H/PR:L/UI:N/S:U/C:H/I:H/A:H
  description: |-
    PLUGIN_NAME allows Groovy scripts to load classpath entries from JAR files, specified either by file path or by URL, which administrators must approve before any script is allowed to use them.

    PLUGIN_NAME 1415.v9a_f9b_3a_c253d and earlier downloads a JAR file specified by URL twice when a script is evaluated, confirming the approval of the first download and loading the classpath entries from the second.

    This results in a time-of-check to time-of-use (TOCTOU) race condition that allows attackers able to define classpath entries to execute arbitrary code in the context of the Jenkins controller JVM.

    NOTE: Exploitation requires an already approved JAR file to be served from an attacker-controlled server.

    PLUGIN_NAME 1422.v06869826dd9b_ downloads a JAR file specified by URL only once when a script is evaluated, confirming the approval of that download and loading the classpath entries from it.

    NOTE: This vulnerability has been reported through the link:/blog/2025/12/10/jenkins-bug-bounty-program-yeswehack-european-commission/[Jenkins Bug Bounty Program sponsored by the European Commission].
  plugins:
  - name: script-security
    previous: 1415.v9a_f9b_3a_c253d
    fixed: 1422.v06869826dd9b_
- id: SECURITY-3977
  title: Sandbox bypass vulnerability in PLUGIN_NAME
  cve: CVE-2026-92129
  cvss:
    severity: High
    vector: CVSS:3.1/AV:N/AC:H/PR:L/UI:N/S:U/C:H/I:H/A:H
  description: |-
    PLUGIN_NAME provides a sandbox feature that allows running user-provided scripts safely by intercepting and checking potentially unsafe operations.

    Groovy allows an existing class to be extended at runtime with additional methods.

    PLUGIN_NAME 1415.v9a_f9b_3a_c253d and earlier does not check calls to such methods from sandboxed scripts.

    This allows attackers with permission to define and run sandboxed scripts, including Pipelines, to bypass the sandbox protection and execute code outside the sandbox.

    NOTE: Exploitation requires that the dynamically added methods have already been defined by code running outside the sandbox, e.g. by a plugin, as sandboxed scripts cannot extend a class themselves.

    PLUGIN_NAME 1422.v06869826dd9b_ rejects calls from sandboxed scripts to methods added dynamically to classes at runtime.
  plugins:
  - name: script-security
    previous: 1415.v9a_f9b_3a_c253d
    fixed: 1422.v06869826dd9b_
- id: SECURITY-3729
  reporter: Babauca
  title: Exposure of System-scoped credentials in PLUGIN_NAME
  cve: CVE-2026-92130
  cvss:
    severity: Low
    vector: CVSS:3.1/AV:N/AC:H/PR:L/UI:N/S:U/C:L/I:N/A:N
  description: |-
    PLUGIN_NAME 841.vec5b_9e1806ec and earlier does not set the appropriate context for credentials lookup in the `resolveScm` Pipeline step, allowing the use of System-scoped credentials otherwise reserved for the global configuration.

    This allows attackers with Item/Configure permission to access and capture credentials they are not entitled to use.

    PLUGIN_NAME 842.v3a_b_59b_57b_e6e defines the appropriate context for credentials lookup in the `resolveScm` Pipeline step.

    NOTE: This vulnerability has been reported through the link:/blog/2025/12/10/jenkins-bug-bounty-program-yeswehack-european-commission/[Jenkins Bug Bounty Program sponsored by the European Commission].
  plugins:
  - name: workflow-multibranch
    previous: 841.vec5b_9e1806ec
    fixed: 842.v3a_b_59b_57b_e6e
- id: SECURITY-3796
  reporter: Samy Medjahed (Ap4sh) & Eliott Laurie (Ethicxz); and, independently, AnBui
  title: Path traversal vulnerability in PLUGIN_NAME
  cve: CVE-2026-92131
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:H/PR:L/UI:N/S:U/C:L/I:L/A:N
  description: |-
    PLUGIN_NAME provides the `library` step to retrieve a shared library from an SCM, using a library path to specify the directory containing the library inside the SCM checkout.

    PLUGIN_NAME 805.va_fc79344957d and earlier does not restrict that library path to a relative path inside the SCM checkout.

    Additionally, it follows symbolic links to locations outside of the SCM checkout when retrieving the library.

    This results in a path traversal vulnerability, allowing attackers able to configure Pipelines to read files in a `resources` directory and to delete files in a `test` directory on the Jenkins controller file system.

    PLUGIN_NAME 806.v408277b_33d1d restricts the library path to a relative path inside the SCM checkout.

    Additionally, it prohibits symbolic links in the library path and inside the library.

    NOTE: This vulnerability has been reported through the link:/blog/2025/12/10/jenkins-bug-bounty-program-yeswehack-european-commission/[Jenkins Bug Bounty Program sponsored by the European Commission].
  plugins:
  - name: pipeline-groovy-lib
    previous: 805.va_fc79344957d
    fixed: 806.v408277b_33d1d
- id: SECURITY-4028
  reporter: Bun (@bunmamd4cb13t)
  title: SSRF vulnerability in PLUGIN_NAME allows capturing the Develocity access
    key
  cve: CVE-2026-92132
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:L/I:L/A:N
  description: |-
    PLUGIN_NAME provides global options for administrators to detect build scan links in build logs, and to show an enriched build scan summary for those build scans on the build page.

    In PLUGIN_NAME 2.19.1252.v15196b_5a_6e10 and earlier, the enriched build scan summary requests data from the build scan link detected in the build log, even when a Develocity server URL is configured in the global configuration.

    This allows attackers able to control the build log to capture the Develocity access key configured in the global configuration by having Jenkins connect to an attacker-specified URL.

    PLUGIN_NAME 2.20.1253.vc116f0763a_eb_ requests build scan data from the Develocity server URL configured in the global configuration, taking only the build scan identifier from the detected build scan link.
  plugins:
  - name: gradle
    previous: 2.19.1252.v15196b_5a_6e10
    fixed: 2.20.1253.vc116f0763a_eb_
- id: SECURITY-3851
  reporter: hai27ii2o; and, independently, Yutaka Sasaki
  title: Cache confusion in PLUGIN_NAME
  cve: CVE-2026-92133
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:L/I:N/A:N
  description: |-
    PLUGIN_NAME allows jobs to override the globally configured GitLab API token credentials with an alternative one, and caches the GitLab API client it builds for those credentials.

    PLUGIN_NAME 1.2149.vcfc32c82b_f7f and earlier derives the cache key from the credentials ID alone, omitting the folder in which the credentials are resolved, resulting in the GitLab API token credentials being taken from the folder of the job that created the cache entry.

    This allows attackers with Item/Configure permission to access GitLab API token credentials they are not entitled to use.

    PLUGIN_NAME 1.2152.veec0897048b_0 derives the cache key from both the credentials ID and the folder in which the credentials are resolved, taking the GitLab API token credentials from the folder of the job making the request.
  plugins:
  - name: gitlab-plugin
    previous: 1.2149.vcfc32c82b_f7f
    fixed: 1.2152.veec0897048b_0
- id: SECURITY-3937
  reporter: vnyuh from CyStack Security
  title: Stored XSS vulnerability in PLUGIN_NAME
  cve: CVE-2026-92134
  cvss:
    severity: High
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:R/S:U/C:H/I:H/A:H
  description: |-
    PLUGIN_NAME uses analysis results IDs to create the links to analysis results on the Jenkins UI.

    PLUGIN_NAME 13.10258.va_17d49a_78c3b_ and earlier does not validate the analysis results ID when a job configuration is submitted through the REST API.
    This allows attackers with Item/Configure permission to use a `javascript:` scheme URL as identifier, resulting in a stored cross-site scripting (XSS) vulnerability.

    TIP: On Jenkins 2.539 and newer, LTS 2.541.1 and newer, enforcing link:/doc/book/security/csp/[Content Security Policy protection] mitigates this vulnerability.

    PLUGIN_NAME 13.10259.v80f407cb_03a_e validates the analysis results ID when a job configuration is loaded, ensuring no link is created with a `javascript:` scheme URL as identifier.
  plugins:
  - name: warnings-ng
    previous: 13.10258.va_17d49a_78c3b_
    fixed: 13.10259.v80f407cb_03a_e
- id: SECURITY-4118
  reporter: 'Ullrich Hafner (GitHub: uhafner)'
  title: Stored XSS vulnerability in PLUGIN_NAME
  cve: CVE-2026-92135
  cvss:
    severity: High
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:R/S:U/C:H/I:H/A:H
  description: |-
    PLUGIN_NAME uses coverage results IDs to create the links to coverage results on the Jenkins UI.

    PLUGIN_NAME 3.3358.v9487dde48783 and earlier does not validate the coverage results ID when a job configuration is submitted through the REST API.
    This allows attackers with Item/Configure permission to use a `javascript:` scheme URL as identifier, resulting in a stored cross-site scripting (XSS) vulnerability.

    TIP: On Jenkins 2.539 and newer, LTS 2.541.1 and newer, enforcing link:/doc/book/security/csp/[Content Security Policy protection] mitigates this vulnerability.

    PLUGIN_NAME 3.3361.v0626103a_67e6 validates the coverage results ID when a job configuration is loaded, ensuring no link is created with a `javascript:` scheme URL as identifier.
  plugins:
  - name: coverage
    previous: 3.3358.v9487dde48783
    fixed: 3.3361.v0626103a_67e6
- id: SECURITY-3992
  reporter: Han, JunGyu
  title: Stored XSS vulnerability in PLUGIN_NAME
  cve: CVE-2026-92136
  cvss:
    severity: High
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:R/S:U/C:H/I:H/A:H
  description: |-
    PLUGIN_NAME 5.6.4 and earlier does not escape CWE values from Dependency-Check reports on the Jenkins UI.

    This results in a stored cross-site scripting (XSS) vulnerability exploitable by attackers with Item/Configure permission.

    TIP: On Jenkins 2.539 and newer, LTS 2.541.1 and newer, enforcing link:/doc/book/security/csp/[Content Security Policy protection] mitigates this vulnerability.

    PLUGIN_NAME 5.6.5 escapes CWE values from Dependency-Check reports.
  plugins:
  - name: dependency-check-jenkins-plugin
    previous: 5.6.4
    fixed: 5.6.5
- id: SECURITY-3971
  reporter: Moath Aloufi, https://github.com/m0xath
  title: Path traversal vulnerability in PLUGIN_NAME
  cve: CVE-2026-92137
  cvss:
    severity: High
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:H
  description: |-
    PLUGIN_NAME 6.2.2 and earlier does not check that the archive directory configured for Robot Framework report files is contained within the build directory on the Jenkins controller.

    This allows attackers with Item/Configure permission to create or replace arbitrary files on the Jenkins controller file system with attacker-specified content, link:/security/vulnerabilities/#writing-files[which can lead to remote code execution].

    PLUGIN_NAME 6.3.0 checks that the archive directory configured for Robot Framework report files is contained within the build directory on the Jenkins controller, and uses the default archive directory instead when it is not.
  plugins:
  - name: robot
    previous: 6.2.2
    fixed: 6.3.0
- id: SECURITY-3778
  reporter: dyingman1 (https://github.com/dyingman1, redpoc Offensive Security Team)
  title: OAuth access token hijacking via callback URL manipulation in PLUGIN_NAME
  cve: CVE-2026-92138
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:H/PR:N/UI:R/S:U/C:L/I:L/A:N
  description: |-
    PLUGIN_NAME implements an OAuth provider for Bitbucket Server.

    The OAuth authorization endpoint in PLUGIN_NAME 6.0.1 and earlier reads the `oauth_callback` URL from the submitted form rather than from the server-side stored request token.
    An attacker who can manipulate the form submission can cause Jenkins to redirect the authorizing user's browser to an arbitrary URL.
    The redirect includes the `oauth_token` and `oauth_verifier` parameters, allowing the attacker to complete the OAuth flow and obtain an access token on behalf of the victim.

    NOTE: Exploiting this vulnerability requires the attacker to have previously registered an OAuth consumer (requires Overall/Administer permission) and to be able to intercept and modify the victim's form submission.

    PLUGIN_NAME 6.0.2 uses the server-side stored request token to determine the callback URL.
  plugins:
  - name: atlassian-bitbucket-server-integration
    previous: 6.0.1
    fixed: 6.0.2
- id: SECURITY-3980
  reporter: Sanghyeon Lee (h9e0n, https://github.com/isanghyeon)
  title: SSRF vulnerability in PLUGIN_NAME allows capturing credentials
  cve: CVE-2026-92139
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:L/A:N
  description: |-
    PLUGIN_NAME provides a webhook endpoint at `/bitbucket-hook/` to receive webhook notifications.

    When acting on these notifications, PLUGIN_NAME 4.0.1 and earlier trusts values provided in the webhook payload, including certain URLs, and uses configured Bitbucket credentials to connect to those URLs.

    This allows attackers to capture Bitbucket credentials stored in Jenkins by sending a crafted webhook payload.

    PLUGIN_NAME 4.1.0 takes the Bitbucket Server endpoint it connects to, and the credentials it uses, from its own configuration rather than from the webhook payload.
  plugins:
  - name: bitbucket-push-and-pull-request
    previous: 4.0.1
    fixed: 4.1.0
- id: SECURITY-4027
  reporter: Bun (@bunmamd4cb13t)
  title: Stored XSS vulnerability in PLUGIN_NAME
  cve: CVE-2026-92140
  cvss:
    severity: High
    vector: CVSS:3.1/AV:N/AC:L/PR:L/UI:R/S:U/C:H/I:H/A:H
  description: |-
    PLUGIN_NAME 1301.v8957053c7902 and earlier does not escape the sender name from Gitee push webhook payloads in build causes.

    This results in a stored cross-site scripting (XSS) vulnerability exploitable by attackers able to trigger builds via the PLUGIN_NAME webhook endpoint.

    PLUGIN_NAME 1304.v2702f1d71cde escapes the sender name from Gitee push webhook payloads in build causes.
  plugins:
  - name: gitee
    previous: 1301.v8957053c7902
    fixed: 1304.v2702f1d71cde
- id: SECURITY-3767
  reporter: dyingman1 (https://github.com/dyingman1, redpoc Offensive Security Team)
  title: Open redirect vulnerability in PLUGIN_NAME
  cve: CVE-2026-92141
  cvss:
    severity: Medium
    vector: CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:N/I:L/A:N
  description: |-
    PLUGIN_NAME 2.4.1 and earlier does not restrict the redirect URL after login.

    This allows attackers to perform phishing attacks by having users go to a Jenkins URL that will forward them to a different site after successful authentication.

    PLUGIN_NAME 2.4.2 only redirects to relative (Jenkins) URLs.
  plugins:
  - name: keycloak
    previous: 2.4.1
    fixed: 2.4.2
