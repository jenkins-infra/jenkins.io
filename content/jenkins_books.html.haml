---
layout: simplepage
title: Jenkins Related Books
---

:css
  .col-md-12 {
    margin-bottom: 2rem;
  }
  #jenkins-related-books {
    margin-bottom: 2rem;
  }

- site.jenkins_books.values.sort_by{ |jenkins_book| [jenkins_book['type'], jenkins_book['title'], jenkins_book['publication_year']] }.reverse.each_with_index do |jenkins_book, index|
  - if jenkins_book['image']
    %img{:src => expand_link('images/' + jenkins_book['image']), :style => 'max-height: 300px; float: right; margin: 5px;'}
  %div{:style => 'margin-bottom: 2rem'}
    %h4
      %span
        = (index + 1).to_s + '.'
        - if jenkins_book['url']
          %a{:href => jenkins_book['url']}
            = jenkins_book['title']
        - else
          = jenkins_book['title']
    - if jenkins_book['author']
      %h5
        Authored by
        = jenkins_book['author']
    - if jenkins_book['authors']
      %h5
        Authored by
        = jenkins_book['authors']
    - if jenkins_book['publisher']
      %h5
        Published by
        = jenkins_book['publisher']
    - if jenkins_book['publication_year']
      %h5
        Published in
        = jenkins_book['publication_year']
    - if jenkins_book['publication_year']
      %h5
        Type:
        = jenkins_book['type']
    - if jenkins_book['description']
      %p
        = jenkins_book['description']
    - if jenkins_book['quote']
      %blockquote
        =jenkins_book['quote']
    %div{:style => 'clear: both;'}