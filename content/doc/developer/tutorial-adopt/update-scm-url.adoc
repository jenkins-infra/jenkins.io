---
layout: developersection
title: Update SCM URL
---

:task-identifier: update-scm-url
:task-description: Use https:// for scm URL

= Update SCM URL

.Update the SCM URL in the SCM section of the pom file
video::Fev8KfFsPZE[youtube,width=800,height=420,start=2961]

GitHub is deprecating one of the unauthenticated access protocols (git:// protocol).
The `pom.xml` section that defines the `scm` for the plugin should refer to the repository with the https:// protocol instead of the git:// protocol

// Create the branch
include::doc/developer/tutorial-adopt/_create-a-branch.adoc[]

== Use https instead of git protocol

Edit the scm section in the pom XML file to replace git:// with https:// .

----
   <scm>
-    <connection>scm:git:git://github.com/jenkinsci/schedule-build-plugin.git</connection>
+    <connection>scm:git:https://github.com/jenkinsci/schedule-build-plugin.git</connection>
   </scm>
----

// Create a pull request
include::doc/developer/tutorial-adopt/_create-a-pull-request.adoc[]
