---
layout: section
title: WAR file
---
ifdef::backend-html5[]
:description:
:author:
:email: jenkinsci-docs@googlegroups.com
:sectanchors:
:toc: left
:toclevels: 4
:hide-uri-scheme:
ifdef::env-github[:imagesdir: ../resources]
ifndef::env-github[:imagesdir: ../../resources]
endif::[]

== WAR File

include::doc/book/updating/_pre-upgrade-wizard.adoc[]

If your Jenkins instance is run with the command 
[source]
```
java -jar jenkins.war
```
# or
[source]
```
java -jar jenkins-oc.war
```
You can simply replace the jenkins.war/jenkins-oc.war file with the latest version 

== Replace the WAR file
The Jenkins Web application ARchive (WAR) file can be started from the command line like this:

. Download the 
link:https://www.jenkins.io/download/[latest Jenkins WAR file]
to an appropriate directory on your machine

. Open up a terminal/command prompt window to the download directory

. Run the command `java -jar jenkins.war` to start your jenkins instance

