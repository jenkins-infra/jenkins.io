---
layout: simplepage
title: Awards
---

- site.awards.values.sort_by { |award| [award['year'], award['name']] }.reverse.each do |award|
  .award-card.mx-auto.my-4
    .award-content.text-left
      %h2.award-title
        - if award['url']
          %a{:href => award['url'], :target => '_blank', :class => 'text-decoration-none'}
            = award['title']
        - else
          = award['title']
      - if award['description']
        %p.award-description
          = award['description']
      - if award['quote']
        %blockquote.award-quote.blockquote
          %p.mb-0
            = award['quote']
    - if award['image']
      .award-image.text-center
        %img{:src => expand_link('images/' + award['image']), :alt => award['title'], :class => 'img-fluid rounded'}
