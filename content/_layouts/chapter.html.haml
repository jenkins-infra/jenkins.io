---
layout: documentation
notitle: true
---

:ruby
  chapter_key = File.basename(File.dirname(page.source_path))
  index = site.handbook.chapters.index { |c| c.key == chapter_key }
  chapter = site.handbook.chapters[index]

  previous_chapter = nil
  next_chapter = nil
  next_is_section = false

  if index > 0
    previous_chapter = site.handbook.chapters[index - 1]
  end

  if chapter.sections && chapter.sections.size > 0
    next_chapter = chapter.sections.first
    next_is_section = true
  elsif index < site.handbook.chapters.size
    next_chapter = site.handbook.chapters[index + 1]
  end

%div
  %div
    .ctc
      .section
        %h1
          = page.title

        = content

%div
  .row
    .col-md-4
      - if previous_chapter
        %a.app-button.app-button--tertiary.prev.doc-page-link{:href =>  File.join('..', previous_chapter.key)}
          %ion-icon{:name => "arrow-back-outline"}
          #{previous_chapter.title}
    .col-md-4
      %a.app-button.app-button--tertiary.index.doc-page-link{:href => '../'}
        Index
    .col-md-4
      - if next_chapter
        - if next_is_section
          %a.app-button.app-button--tertiary.next.doc-page-link{:href =>  next_chapter.key}
            #{next_chapter.title}
            %ion-icon{:name => "arrow-forward-outline"}
        - else
          %a.app-button.app-button--tertiary.next.doc-page-link{:href =>  File.join('..', next_chapter.key)}
            #{next_chapter.title}
            %ion-icon{:name => "arrow-forward-outline"}

%div
= partial ('_feedback-footer.html')
