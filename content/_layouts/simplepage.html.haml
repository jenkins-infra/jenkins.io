---
layout: default
---

:css
  .toc {
      float: right;
  }

- unless page.noanchors
  :javascript
    $(function () {
      for (var i = 1 ; i <= 6 ; i ++) {
        anchors.add('.container .row.body h' + i);
      }
    })

.container
  .row.body
    .col-md-12
      - unless page.notitle
        %h1
          = page.title
      = content
