==== Windows 2019 controller images have been dropped

For users who must use Windows for their controller and are stuck on Windows
2019, the workaround is either to:

. Install Jenkins controller via the MSI available via the "Windows" links
on link:/download/[].
. Build their own image.
