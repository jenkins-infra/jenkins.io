---
layout: default
title: 'Jenkins Blog'
---

- for post in page.posts
  .post
    %h1
      %a{:href => post.url}
        = post.title

    %span.submitted
      Published on
      = post.date
      by
      = display_author_for(post)

    .content
      != post.content

= page.posts.links
