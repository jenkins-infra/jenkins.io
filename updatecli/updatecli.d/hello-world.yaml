---
name: Bump the various docker images versions for the Hello World! tutorial

scms:
  default:
    kind: github
    spec:
      user: "{{ .github.user }}"
      email: "{{ .github.email }}"
      owner: "{{ .github.owner }}"
      repository: "{{ .github.repository }}"
      token: "{{ requiredEnv .github.token }}"
      username: "{{ .github.username }}"
      branch: "{{ .github.branch }}"

sources:
  nodeAlpineLatestVersion:
    kind: githubrelease
    spec:
      owner: "nodejs"
      repository: "node"
      token: "{{ requiredEnv .github.token }}"
      username: "{{ .github.username }}"
    transformers:
      - trimprefix: "v"
  mavenTemurinAlpineLatestVersion:
    kind: githubrelease
    spec:
      owner: "apache"
      repository: "maven"
      token: "{{ requiredEnv .github.token }}"
      username: "{{ .github.username }}"
    transformers:
      - trimprefix: "maven-"
  rubyAlpineLatestVersion:
    kind: githubrelease
    spec:
      owner: "ruby"
      repository: "ruby"
      token: "{{ requiredEnv .github.token }}"
      username: "{{ .github.username }}"
    transformers:
      - trimprefix: "v"
      - replacer:
          from: "_"
          to: "."
  pythonAlpineLatestVersion:
    kind: dockerdigest
    spec:
      image: "python"
      tag: "3-alpine"
  phpAlpineLatestVersion:
    kind: dockerdigest
    spec:
      image: "php"
      tag: "alpine"
  golangAlpineLatestVersion:
    kind: dockerdigest
    spec:
      image: "golang"
      tag: "1-alpine"
conditions:
  testNodeTemurinAlpineImagePublished:
    name: "Test node:<latest_version>-alpine docker image tag"
    kind: dockerimage
    sourceid: nodeAlpineLatestVersion
    spec:
      image: "node"
    transformers:
      - addsuffix: "-alpine"
  testNodeAlpineArg:
    name: "Does the 'Hello World!' tutorial have a reference to the node alpine image?"
    kind: file
    disablesourceinput: true
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        .*docker.*image.*\'node:.*
  testMavenTemurinAlpineImagePublished:
    name: "Test maven:<latest_version>-eclipse-temurin-17 docker image tag"
    kind: dockerimage
    sourceid: mavenTemurinAlpineLatestVersion
    spec:
      image: "maven"
    transformers:
      - addsuffix: "-eclipse-temurin-17"
  testMavenTemurinAlpineArg:
    name: "Does the 'Hello World!' tutorial have a reference to the mvn temurin image?"
    kind: file
    disablesourceinput: true
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        .*agent.*docker.*image.*maven:.*
  testRubyAlpineImagePublished:
    name: "Test ruby:<latest_version>-eclipse-temurin-17 docker image tag"
    kind: dockerimage
    sourceid: rubyAlpineLatestVersion
    spec:
      image: "ruby"
    transformers:
      - addsuffix: "-alpine"
  testRubyAlpineArg:
    name: "Does the 'Hello World!' tutorial have a reference to the ruby alpine image?"
    kind: file
    disablesourceinput: true
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        .*agent.*docker.*image.*ruby:.*
  testPythonAlpineArg:
    name: "Does the 'Hello World!' tutorial have a reference to the Python alpine image?"
    kind: file
    disablesourceinput: true
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        .*agent.*docker.*image.*python:.*
  testPhpAlpineArg:
    name: "Does the 'Hello World!' tutorial have a reference to the php alpine image?"
    kind: file
    disablesourceinput: true
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        .*agent.*docker.*image.*php:.*
  testGolangAlpineArg:
    name: "Does the 'Hello World!' tutorial have a reference to the golang alpine image?"
    kind: file
    disablesourceinput: true
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        .*agent.*docker.*image.*golang:.*

targets:
  updateHelloWorldTutorialNodePipeline:
    name: "Update the value of the node docker image for pipelines in the 'Hello World!' tutorial"
    kind: file
    sourceid: nodeAlpineLatestVersion
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        (.*agent.*docker.*image.*\')node:(.*)(\'.*)
      # like regex? Try them at https://regex101.com/ and select golang and substitution
      replacepattern: >-
        ${1}node:{{ source "nodeAlpineLatestVersion" }}-alpine${3}
    scmid: default
  updateHelloWorldTutorialNodeScripted:
    name: "Update the value of the node docker image for scripts in the 'Hello World!' tutorial"
    kind: file
    sourceid: nodeAlpineLatestVersion
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        (.*docker.*image.*\')node:(.*)(\'.*)
      # like regex? Try them at https://regex101.com/ and select golang and substitution
      replacepattern: >-
        ${1}node:{{ source "nodeAlpineLatestVersion" }}-alpine${3}
    scmid: default
  updateHelloWorldTutorialMavenPipeline:
    name: "Update the value of the node docker image for pipelines in the 'Hello World!' tutorial"
    kind: file
    sourceid: mavenTemurinAlpineLatestVersion
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        (.*agent.*docker.*image.*\')maven:(.*)(\'.*)
      # like regex? Try them at https://regex101.com/ and select golang and substitution
      replacepattern: >-
        ${1}maven:{{ source "mavenTemurinAlpineLatestVersion" }}-eclipse-temurin-17${3}
    scmid: default
  updateHelloWorldTutorialMavenScripted:
    name: "Update the value of the maven docker image for scripts in the 'Hello World!' tutorial"
    kind: file
    sourceid: mavenTemurinAlpineLatestVersion
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        (.*docker.*image.*\')maven:(.*)(\'.*)
      # like regex? Try them at https://regex101.com/ and select golang and substitution
      replacepattern: >-
        ${1}maven:{{ source "mavenTemurinAlpineLatestVersion" }}-eclipse-temurin-17${3}
    scmid: default
  updateHelloWorldTutorialRubyPipeline:
    name: "Update the value of the ruby docker image for pipelines in the 'Hello World!' tutorial"
    kind: file
    sourceid: rubyAlpineLatestVersion
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        (.*agent.*docker.*image.*\')ruby:(.*)(\'.*)
      # like regex? Try them at https://regex101.com/ and select golang and substitution
      replacepattern: >-
        ${1}ruby:{{ source "rubyAlpineLatestVersion" }}-alpine${3}
    scmid: default
  updateHelloWorldTutorialRubyScripted:
    name: "Update the value of the ruby docker image for scripts in the 'Hello World!' tutorial"
    kind: file
    sourceid: rubyAlpineLatestVersion
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        (.*docker.*image.*\')ruby:(.*)(\'.*)
      # like regex? Try them at https://regex101.com/ and select golang and substitution
      replacepattern: >-
        ${1}ruby:{{ source "rubyAlpineLatestVersion" }}-alpine${3}
    scmid: default
  updateHelloWorldTutorialPythonPipeline:
    name: "Update the value of the python docker image for pipelines in the 'Hello World!' tutorial"
    kind: file
    sourceid: pythonAlpineLatestVersion
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        (.*agent.*docker.*image.*\')python:(.*)(\'.*)
      # like regex? Try them at https://regex101.com/ and select golang and substitution
      replacepattern: >-
        ${1}python:{{ source "pythonAlpineLatestVersion" }}${3}
    scmid: default
  updateHelloWorldTutorialPythonScripted:
    name: "Update the value of the python docker image for scripts in the 'Hello World!' tutorial"
    kind: file
    sourceid: pythonAlpineLatestVersion
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        (.*docker.*image.*\')python:(.*)(\'.*)
      # like regex? Try them at https://regex101.com/ and select golang and substitution
      replacepattern: >-
        ${1}python:{{ source "pythonAlpineLatestVersion" }}${3}
    scmid: default
  updateHelloWorldTutorialPhpPipeline:
    name: "Update the value of the php docker image for pipelines in the 'Hello World!' tutorial"
    kind: file
    sourceid: phpAlpineLatestVersion
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        (.*agent.*docker.*image.*\')php:(.*)(\'.*)
      # like regex? Try them at https://regex101.com/ and select golang and substitution
      replacepattern: >-
        ${1}php:{{ source "phpAlpineLatestVersion" }}${3}
    scmid: default
  updateHelloWorldTutorialPhpScripted:
    name: "Update the value of the php docker image for scripts in the 'Hello World!' tutorial"
    kind: file
    sourceid: phpAlpineLatestVersion
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        (.*docker.*image.*\')php:(.*)(\'.*)
      # like regex? Try them at https://regex101.com/ and select golang and substitution
      replacepattern: >-
        ${1}php:{{ source "phpAlpineLatestVersion" }}${3}
    scmid: default
  updateHelloWorldTutorialGolangPipeline:
    name: "Update the value of the golang docker image for pipelines in the 'Hello World!' tutorial"
    kind: file
    sourceid: golangAlpineLatestVersion
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        (.*agent.*docker.*image.*\')golang:(.*)(\'.*)
      # like regex? Try them at https://regex101.com/ and select golang and substitution
      replacepattern: >-
        ${1}golang:{{ source "golangAlpineLatestVersion" }}${3}
    scmid: default
  updateHelloWorldTutorialGolangScripted:
    name: "Update the value of the golang docker image for scripts in the 'Hello World!' tutorial"
    kind: file
    sourceid: golangAlpineLatestVersion
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        (.*docker.*image.*\')golang:(.*)(\'.*)
      # like regex? Try them at https://regex101.com/ and select golang and substitution
      replacepattern: >-
        ${1}golang:{{ source "golangAlpineLatestVersion" }}${3}
    scmid: default

actions:
  default:
    kind: github/pullrequest
    scmid: default
    title: Bump node alpine docker image version to {{ source "nodeAlpineLatestVersion" }}
    spec:
      labels:
        - dependencies