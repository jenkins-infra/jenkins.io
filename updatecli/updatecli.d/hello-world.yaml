---
name: Bump the various docker images versions for the Hello World! tutorial

scms:
  default:
    kind: github
    spec:
      user: "{{ .github.user }}"
      email: "{{ .github.email }}"
      owner: "{{ .github.owner }}"
      repository: "{{ .github.repository }}"
      token: "{{ requiredEnv .github.token }}"
      username: "{{ .github.username }}"
      branch: "{{ .github.branch }}"

sources:
  nodeAlpineLatestVersion:
    kind: dockerdigest
    spec:
      image: "node"
      tag: "alpine"
  mavenTemurinAlpineLatestVersion:
    kind: dockerdigest
    spec:
      image: "maven"
      tag: "3-eclipse-temurin-17"
  rubyAlpineLatestVersion:
    kind: dockerdigest
    spec:
      image: "ruby"
      tag: "alpine"
conditions:
  testNodeAlpineArg:
    name: "Does the 'Hello World!' tutorial have a reference to the node alpine image?"
    kind: file
    disablesourceinput: true
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        .*docker.*image.*\'node:.*
  testMavenTemurinAlpineArg:
          name: "Does the 'Hello World!' tutorial have a reference to the mvn temurin image?"
          kind: file
          disablesourceinput: true
          spec:
            file: content/doc/pipeline/tour/hello-world.adoc
            matchpattern: >-
              .*agent.*docker.*image.*maven:.*
  testRubyTemurinAlpineArg:
    name: "Does the 'Hello World!' tutorial have a reference to the ruby alpine image?"
    kind: file
    disablesourceinput: true
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        .*agent.*docker.*image.*ruby:.*

targets:
  updateHelloWorldTutorialNodePipeline:
    name: "Update the value of the node docker image for pipelines in the 'Hello World!' tutorial"
    kind: file
    sourceid: nodeAlpineLatestVersion
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        (.*agent.*docker.*image.*\')node:(.*)(\'.*)
      # like regex? Try them at https://regex101.com/ and select golang and subsitution
      replacepattern: >-
        ${1}node:{{ source "nodeAlpineLatestVersion" }}${3}
    scmid: default
  updateHelloWorldTutorialNodeScripted:
    name: "Update the value of the node docker image for scripts in the 'Hello World!' tutorial"
    kind: file
    sourceid: nodeAlpineLatestVersion
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        (.*docker.*image.*\')node:(.*)(\'.*)
      # like regex? Try them at https://regex101.com/ and select golang and subsitution
      replacepattern: >-
        ${1}node:{{ source "nodeAlpineLatestVersion" }}${3}
    scmid: default
  updateHelloWorldTutorialMavenPipeline:
    name: "Update the value of the node docker image for pipelines in the 'Hello World!' tutorial"
    kind: file
    sourceid: mavenTemurinAlpineLatestVersion
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        (.*agent.*docker.*image.*\')maven:(.*)(\'.*)
      # like regex? Try them at https://regex101.com/ and select golang and subsitution
      replacepattern: >-
        ${1}maven:{{ source "mavenTemurinAlpineLatestVersion" }}${3}
    scmid: default
  updateHelloWorldTutorialMavenScripted:
    name: "Update the value of the maven docker image for scripts in the 'Hello World!' tutorial"
    kind: file
    sourceid: mavenTemurinAlpineLatestVersion
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        (.*docker.*image.*\')maven:(.*)(\'.*)
      # like regex? Try them at https://regex101.com/ and select golang and subsitution
      replacepattern: >-
        ${1}maven:{{ source "nodeAlpineLatestVersion" }}${3}
    scmid: default
  updateHelloWorldTutorialRubyPipeline:
    name: "Update the value of the ruby docker image for pipelines in the 'Hello World!' tutorial"
    kind: file
    sourceid: rubyAlpineLatestVersion
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        (.*agent.*docker.*image.*\')ruby:(.*)(\'.*)
      # like regex? Try them at https://regex101.com/ and select golang and subsitution
      replacepattern: >-
        ${1}ruby:{{ source "rubyAlpineLatestVersion" }}${3}
    scmid: default
  updateHelloWorldTutorialRubyScripted:
    name: "Update the value of the ruby docker image for scripts in the 'Hello World!' tutorial"
    kind: file
    sourceid: rubyAlpineLatestVersion
    spec:
      file: content/doc/pipeline/tour/hello-world.adoc
      matchpattern: >-
        (.*docker.*image.*\')ruby:(.*)(\'.*)
      # like regex? Try them at https://regex101.com/ and select golang and subsitution
      replacepattern: >-
        ${1}ruby:{{ source "rubyAlpineLatestVersion" }}${3}
    scmid: default

actions:
  default:
    kind: github/pullrequest
    scmid: default
    title: Bump node alpine docker image version to {{ source "nodeAlpineLatestVersion" }}
    spec:
      labels:
        - dependencies